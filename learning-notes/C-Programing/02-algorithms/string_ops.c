#include <stdio.h>
#include <string.h>
#include <stdbool.h>

// Function prototypes
void reverse_string(char* str);
bool is_palindrome(char* str);
int my_atoi(const char* str);
int my_strlen(const char* str);
void my_strcpy(char* dest, const char* src);
int my_strcmp(const char* s1, const char* s2);

// TODO: Implement all functions without using stdlib string functions

void reverse_string(char* str) {
    // TODO: Reverse string in-place
}

bool is_palindrome(char* str) {
    // TODO: Check if string is palindrome (ignore case)
    return false;
}

int my_atoi(const char* str) {
    // TODO: Convert string to integer
    // Handle: leading spaces, +/- sign, overflow
    return 0;
}

int my_strlen(const char* str) {
    // TODO: Calculate string length
    return 0;
}

void my_strcpy(char* dest, const char* src) {
    // TODO: Copy string from src to dest
}

int my_strcmp(const char* s1, const char* s2) {
    // TODO: Compare two strings
    // Return 0 if equal, <0 if s1<s2, >0 if s1>s2
    return 0;
}

int main() {
    // TODO: Test your implementations
    
    return 0;
}
